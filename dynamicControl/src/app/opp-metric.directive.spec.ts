import { OppMetricDirective } from './opp-metric.directive';

describe('OppMetricDirective', () => {
  it('should create an instance', () => {
    const directive = new OppMetricDirective();
    expect(directive).toBeTruthy();
  });
});
